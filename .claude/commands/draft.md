---
description: Break the idea in docs/draft.md into candidate features (launches sdd-drafter)
---

Launch the **`sdd-drafter`** subagent (Agent tool) to analyze `docs/draft.md` and propose candidate
features with a build order.

When it returns: present the candidate features and open questions to the user, ask any blocking
questions, and recommend `/specify` for the first feature.
